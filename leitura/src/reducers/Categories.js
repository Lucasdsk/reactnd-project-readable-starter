
const prevState = {
  categories: [],
};

export default function (state = prevState, action) {
  switch (action) {
    case 'ACION':
      return state;

    default:
      return state;
  }
}
